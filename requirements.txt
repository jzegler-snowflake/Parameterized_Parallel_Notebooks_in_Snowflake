# Python dependencies for carbon emissions notebook
# Use with: REQUIREMENTS_FILE = 'requirements.txt'
# Note: pandas, numpy, and snowflake-snowpark-python are pre-installed in Container Runtime
# Only need to install additional packages not in the base image

tabulate